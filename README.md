# ExecAI (Capstone) – Skeleton App

## What this is
Minimal foundation for the ExecAI project:
- Streamlit frontend
- FastAPI backend

## Setup (Mac)
```bash
git clone <REPO_URL>
cd execai
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

